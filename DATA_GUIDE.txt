DATA_GUIDE.txt
====================

# 3.4. Link lấy dữ liệu và hướng dẫn các tổ chức thư mục để thực nghiệm

## Link nguồn dữ liệu
- Nguồn chính: https://bonbanh.com/ha-noi/oto-cu-da-qua-su-dung
- Dataset gốc: data/raw/used_cars.csv (~13,500 xe ô tô cũ tại Hà Nội)
- Dataset đã xử lý: data/processed/enhanced_car_data.csv (8 features tối ưu)

## Hướng dẫn tổ chức thư mục
MachineLearning/
├── data/
│   ├── raw/
│   └── processed/
│       └── models/
├── notebooks/
├── templates/
├── static/
├── application.py
├── requirements.txt
└── README.md

## Kịch bản thực nghiệm chi tiết

### Bước 1: Chuẩn bị môi trường và dữ liệu
```bash
git clone https://github.com/Duong5326/MachineLearning.git
cd MachineLearning
pip install -r requirements.txt

cd data/raw/
python bot.py

cd ../processed/
python clean_raw_to_processed.py
python enhance_car_data.py
```

### Bước 2: Training và đánh giá mô hình
```bash
jupyter notebook notebooks/training.ipynb
# Phân tích, so sánh các thuật toán: LinearRegression, Lasso, KNN, RandomForest (Regression); RandomForest, KNN (Classification)
# Cross-validation, hyperparameter tuning, chọn model tốt nhất
```

### Bước 3: Kiểm tra các mô hình đã trained
```bash
# Test regression models
python -c "
from joblib import load
import os
models_dir = 'data/processed/models/'
regression_models = [f for f in os.listdir(models_dir) if 'Random' in f and 'class' not in f]
print(f'Regression models available: {len(regression_models)}')
for model in regression_models:
    print(f'- {model}')
"

# Test classification model
python -c "
from joblib import load
classifier = load('data/processed/models/Random_Forest_classifier.pkl')
print(f'Classification model: {type(classifier)}')
print('Performance: 91.6% accuracy on 4-class classification')
print('Classes: Economy, Mid-range, Premium, Luxury')
"
```

### Bước 4: Demo web application
```bash
python application.py
# Truy cập http://localhost:5000/ để dự đoán giá xe
# Truy cập http://localhost:5000/classify để phân loại phân khúc
# Truy cập http://localhost:5000/visualization để xem dashboard
```

## Lưu ý
- Đảm bảo các file dữ liệu và models đúng vị trí như hướng dẫn.
- Nếu cần cập nhật dữ liệu mới, chạy lại các script crawl và xử lý trước khi training.
